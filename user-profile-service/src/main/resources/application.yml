server:
    context-path: /user
    port: 8086
spring:
    application:
        name: BootMongo
    data:
        mongodb:
            database: signup
            host: localhost
            port: 27017
    rabbitmq:
            host: localhost
            port: 5672